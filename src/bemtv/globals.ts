import { CSSInJS } from "../css-in-js/main";

import { LIBRARY_NAME } from "../globals";

export const TAG_HOST_NAME = `${LIBRARY_NAME}--host`;

export const REGEX_COMPONENT_NAME = /\b[A-Z][\w|:]*?\[/;

export const BEMTEVI_CSS_IN_JS = new CSSInJS();

export const AVOIDS_EMPTY_TEMPLATE = " &nbsp; ";
